name: Deploy Learnify

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  # We'll add jobs here in later steps
  test:
    name: Test Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Print a message
        run: echo "Workflow is working!"